# Minimal requirements for Atlas V1 ART without OpenPipe
# Use this if you have issues with openpipe-art installation

# Core dependencies
langchain>=0.2.0
langgraph>=0.1.0
langchain-anthropic>=0.1.0
langchain-openai>=0.1.0  # For OpenRouter and OpenAI support
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Training and evaluation (without OpenPipe ART)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
tqdm>=4.65.0

# For trajectory storage
jsonlines>=3.1.0

# Note: This configuration will work for trajectory capture and basic 
# reward calculation, but won't have OpenPipe ART integration.
# The agent will still function with OpenRouter/Anthropic/OpenAI.