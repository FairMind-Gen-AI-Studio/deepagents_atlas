# OpenPipe ART Integration Requirements
# This builds on top of the base Atlas V1 requirements

# Core dependencies from Atlas V1
langchain>=0.2.0
langgraph>=0.1.0
langchain-anthropic>=0.1.0
langchain-openai>=0.1.0  # For OpenRouter and OpenAI support
# langchain-litellm not needed - OpenRouter covers all models
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# OpenPipe ART specific
# Note: Installing without 'backend' extra due to bitsandbytes compatibility issues
# The core functionality and langgraph integration still work
openpipe-art[langgraph]>=0.4.9
# If you need the backend features, try: pip install openpipe-art[backend] --no-deps

# Training and evaluation
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
tqdm>=4.65.0

# For trajectory storage
jsonlines>=3.1.0